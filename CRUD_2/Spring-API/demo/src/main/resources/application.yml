---

application:
  audience: https://dev-9mdmqlnu.us.auth0.com/api/v2/
  client-origin-url: http://localhost:3000

server:
  error:
    whitelabel:
      enabled: false
  port: 8080

spring:
  mvc:
    throw-exception-if-no-handler-found: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-9mdmqlnu.us.auth0.com/
  web:
    resources:
      add-mappings: false

...
